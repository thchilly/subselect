"""Multi-objective subset selection — Phase 2 stub.

Phase 0 leaves this empty. Phase 2 implements the scalarized weighted-sum cost
(default) and the Pareto front (diagnostic), plus subset search over C(35, k)
for small k and greedy + swap for larger k. See docs/cost_function.md.
"""
