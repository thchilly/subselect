"""Multi-objective subset selection (placeholder).

Will host the scalarized weighted-sum cost (default) and a Pareto-front
diagnostic, plus subset search over ``C(35, k)`` for small ``k`` and a
greedy + swap heuristic for larger ``k``. See ``docs/cost_function.md``.
"""
