"""Taylor diagram — consolidated from the duplicated copies in legacy/.

M9 ports `legacy/cmip6-greece/TaylorDiagram.py` (Yannick Copin's class) verbatim;
the byte-identical climpact/ copy is dropped. `legacy/climpact/TaylorDiagramCRMSE.py`
is retained behind a `DeprecationWarning` because its declared `crmse` parameter
is never consumed.
"""
